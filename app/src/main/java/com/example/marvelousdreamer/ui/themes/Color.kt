package com.example.marvelousdreamer.ui.themes

import androidx.compose.ui.graphics.Color

// Backgrounds — negro profundo con tinte púrpura
val BgDeep      = Color(0xFF0A0710)   // fondo más oscuro
val BgBase      = Color(0xFF100D1A)   // fondo principal de pantallas
val CardSurface = Color(0xFF1A1528)
val BgElevated  = Color(0xFF231D35)   // cards elevadas / dropdowns
val BgOutline   = Color(0xFF2E2647)   // bordes / divisores

// Morado — brand principal
val Violet      = Color(0xFF7C3AED)   // primario
val VioletLight = Color(0xFF9F67F5)   // hover / lighter
val VioletSoft  = Color(0xFFB794F6)   // textos secundarios sobre violet

// Verde — accent
val Emerald     = Color(0xFF10B981)   // accent principal
val EmeraldLight= Color(0xFF34D399)   // hover / lighter
val EmeraldSoft = Color(0xFF6EE7B7)   // muted green

// Gradient stops — violet → emerald
val GradStart   = Color(0xFF6D28D9)
val GradMid     = Color(0xFF4F46E5)
val GradEnd     = Color(0xFF059669)

// Semantic
val Amber       = Color(0xFFF59E0B)   // warning / top badge
val Rose        = Color(0xFFEF4444)   // error / delete

// Text
val Snow        = Color(0xFFFFFFFF)
val Mist        = Color(0xFFD4BBFF)   // text secundari amb tint morat
val Fog         = Color(0xFF6B5F8A)   // text apagat